<!DOCTYPE html>
<html xmlns:border-style="http://www.w3.org/1999/xhtml">
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body data-spy="scroll" data-target=".navbar"data-offset="50">
<div>
    <%= image_tag "tdgs_logo.png" %>
</div>
			<nav class="navbar navbar-default navbar-inverse" data-spy="affix" data-offset-top="115"  role="navigation">
				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
					<ul class="nav navbar-nav">
						<li class="dropdown">
							 <a href="#" class="dropdown-toggle" data-toggle="dropdown">Filtrer<strong class="caret"></strong></a>
							<ul class="dropdown-menu">
								<%
									Type.all.each do |type|
								%>
								<li>
									<a href="/recipes?type=<%= type.id %> "><%= type.name %></a>
								</li>
								<% end %>
								<li class="divider">
								</li>
								<%
									Commodity.all.each do |commodity|
								%>
								<li>
									<a href="/recipes?commodity=<%= commodity.id %> "><%= commodity.name %></a>
								</li>
								<% end %>
								<li class="divider">
								</li>
								<%
									Category.all.each do |category|
								%>
								<li>
									<a href="/recipes?category=<%= category.id %> "><%= category.name %></a>
								</li>
								<% end %>
							</ul>
						</li>
					</ul>

						<%= form_tag recipes_path, :method => 'get', :class => 'navbar-form navbar-left' do %>
						<div class="form-group">
							<%= text_field_tag :q, params[:q], :class => 'form-control' %>
						</div> <button type="submit" class="btn btn-default">Søk</button>
						<% end %>
 recipe_path, :method => 'get' role="search" do>
					<ul class="nav navbar-nav navbar-right">
						<% if signed_in? %>
						<li id="fat-menu" class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown">
								Konto <b class="caret"></b>
							</a>
							<ul class="dropdown-menu">
								<li><%= link_to "Profil", current_user %></li>
								<li class="divider"></li>
								<li>
									<%= link_to "Logg ut", signout_path, method: "delete" %>
								</li>
							</ul>
						</li>
						<% else %>
						<li><%= link_to "Logg inn", signin_path %></li>
						<% end %>
					</ul>
				</div>
			</nav>

	      <% flash.each do |key, value| %>
	        <div class="alert alert-<%= key %>"><%= value %></div>
	      <% end %>
	  <%= yield %>  
	  <%= debug(params) if Rails.env.development? %>
</body>
</html>
